#include <bits/stdc++.h>
using namespace std;

int main()
{
    int nums[] = {2,9};

   int size = sizeof(nums)/sizeof(nums[0]);
       int sum =  nums[0] + nums[size - 1];
       cout << sum;

    return 0;
}
